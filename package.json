{
  "name": "agents-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Tooling and validation scripts for the Copilot Agents Registry.",
  "scripts": {
    "build": "node scripts/build.js",
    "validate": "node scripts/validate.js"
  },
  "devDependencies": {
    "markdownlint": "^0.40.0"
  }
}
