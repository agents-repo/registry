name: Package submission
description: Submit a new agent or flow package to the registry.
title: "package: "
labels:
  - package
body:
  - type: markdown
    attributes:
      value: |
        Use this form to submit a new package under packages/.
        A package may contain agents, flows, or both.
        Submitted package content must be licensed under MIT.

        **Branch name:** `package/<issue-number>-<package-name>`
        **Example:** `package/56-my-package-name`
        Create this issue first to get the issue number, then open the branch.

  - type: checkboxes
    id: package-type
    attributes:
      label: Package contents
      description: >-
        Select all that apply. A package must contain at least one per
        specs/package-format.md.
      options:
        - label: Contains agents (`agents/` directory)
          required: false
        - label: Contains flows (`flows/` directory)
          required: false
    validations:
      required: true

  - type: input
    id: package-name
    attributes:
      label: Package name
      description: Must be lowercase kebab-case.
      placeholder: my-package-name
    validations:
      required: true

  - type: textarea
    id: package-description
    attributes:
      label: Package description
      description: >-
        Required field in metadata.json. 1 to 300 characters.
      placeholder: Short description of what this package does.
    validations:
      required: true

  - type: input
    id: package-owner
    attributes:
      label: Owner
      description: >-
        GitHub username or organization slug. Required field in
        metadata.json.
      placeholder: my-org
    validations:
      required: true

  - type: input
    id: package-homepage
    attributes:
      label: Homepage URL
      description: HTTPS URL. Required field in metadata.json.
      placeholder: https://github.com/my-org/my-repo
    validations:
      required: true

  - type: input
    id: package-repository
    attributes:
      label: Repository URL
      description: HTTPS URL to source repository. Required field in metadata.json.
      placeholder: https://github.com/my-org/my-repo
    validations:
      required: true

  - type: input
    id: package-tags
    attributes:
      label: Tags
      description: >-
        1 to 20 lowercase tags, comma-separated. Required field in
        metadata.json.
      placeholder: productivity, review, automation
    validations:
      required: true

  - type: textarea
    id: package-path
    attributes:
      label: Proposed path
      description: Path under packages/.
      placeholder: packages/my-package-name/
    validations:
      required: true

  - type: textarea
    id: contents
    attributes:
      label: Package files
      description: List expected files in the package.
      placeholder: |
        agents/
          <agent-id>.agent.md
          <agent-id>.metadata.json
        flows/  (if applicable)
          <flow-id>.agent.md
          <flow-id>.metadata.json
        metadata.json
        versions/
          <version>/
            metadata.json
            agents/
              <agent-id>.agent.md
              <agent-id>.metadata.json
            flows/  (if applicable)
              <flow-id>.agent.md
              <flow-id>.metadata.json
            <version>.zip
            <version>-src.zip
          manifest.json
    validations:
      required: true

  - type: textarea
    id: release
    attributes:
      label: Release metadata
      description: >-
        Provide required package metadata timestamps from metadata.json,
        plus the initial version and artifact details for the manifest.
      placeholder: |
        - metadata.createdAt: <RFC 3339 timestamp, e.g. 2026-05-02T00:00:00Z>
        - metadata.updatedAt: <RFC 3339 timestamp, e.g. 2026-05-02T00:00:00Z>
        - version: 1.0.0
        - artifact: 1.0.0.zip
        - sha256: <64-char-lowercase-hex>
        - srcArtifact: 1.0.0-src.zip
        - srcSha256: <64-char-lowercase-hex>
        - manifest.createdAt: <RFC 3339 timestamp, e.g. 2026-05-02T00:00:00Z>
    validations:
      required: true

  - type: dropdown
    id: license
    attributes:
      label: Package license
      description: Submitted package content is accepted only under MIT.
      options:
        - MIT
    validations:
      required: true

  - type: checkboxes
    id: submission-checks
    attributes:
      label: Checklist
      options:
        - label: I followed specs/package-format.md.
          required: true
        - label: I followed specs/agent-format.md for any files under agents/.
        - label: I followed specs/flow-format.md for any files under flows/.
        - label: I followed specs/metadata-schema.md.
          required: true
        - label: I followed specs/manifest-schema.md.
          required: true
        - label: I followed specs/versioning-rules.md for the initial release.
          required: true
        - label: The submitted package content is licensed under MIT.
          required: true
        - label: I authored this content or have the right to submit it under MIT.
          required: true
        - label: I did not include third-party content unless it is licensed under MIT.
          required: true
        - label: ZIP content includes only required files.
          required: true
        - label: >-
            metadata.json includes valid createdAt and updatedAt
            RFC 3339 timestamps.
          required: true
        - label: manifest.json version entries include a valid createdAt timestamp.
          required: true
