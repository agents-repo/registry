# hello-agent

Hello Agent package scaffolded by package-create.

## Quickstart

This package demonstrates a minimal multi-agent setup in the registry.

## Package Contents

- Agents:
  - `hello-agent`
  - `hello-again`
- Flows:
  - `hello-agents`

## Validate and Build

Run from repository root:

```bash
npm run package:validate -- --package hello-agent
npm run package:build -- --package hello-agent
npm run package:validate-artifacts -- --package hello-agent --version 1.0.0
```
